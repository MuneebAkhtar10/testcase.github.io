html(lang='en')
  head
  body
    title FoodSetGo
    meta(name='viewport', content='width=device-width, initial-scale=1')
    meta(http-equiv='Content-Type', content='text/html; charset=utf-8')
    link(rel='stylesheet', href='/stylesheets/feedback.css')
    //- link(href='feedback.css', rel='stylesheet', type='text/css', media='all')
    link(href='//fonts.googleapis.com/css?family=Roboto:400,100,300,500,700,900', rel='stylesheet', type='text/css')
    script.
      function validation() {
      var x = document.myform.email.value;
      var atposition = x.indexOf("@");
      var dotposition = x.lastIndexOf(".");
      if (atposition < 1 || dotposition < atposition + 2 || dotposition + 2 >= x.length) {
      alert("Please enter a valid e-mail address ");
      return false;
      }
      var z = document.myform.name.value;
      var zz = z.charAt(0);
      if (zz=='@'|| zz=='-' || zz=='.')
      {
      alert("Enter a valid name");
      return false;
      }
      var y = document.myform.name.value;
      if (y == "") {
      alert("Please enter your name");
      return false;
      }
      if (!isNaN(y)) {
      alert("Please enter only characters");
      return false;
      }
      }
    // banner
    #pic
      .center-container
        .main
          h1.head FEEDBACK
          .main_grid
            form.form_post(action='/feedback/add', name='myform', method='post', onsubmit='return validation()')
              .agileits_main_grid.l_main_grid
                span.agileits_grid
                  label Your Name 
                  input(type='text', name='name', placeholder='Your Name', required='')
              .agileits_main_grid.l_main_grid
                span.agileits_grid
                  label Your Email 
                  input(type='text', name='email', placeholder='Your Email', required='')
              .agileits_main_grid.l_main_grid
                span.agileits_grid
                  label Resturant Name 
                  input(type='text', name='restname', placeholder='Resturant Name', required='')
              div
                textarea#com(name='comment', rows='5', cols='40', placeholder='Comment')                              
              .main_grid2
                .main_grid2_right
                  input(type='Submit', value='Submit')
      // //footer
